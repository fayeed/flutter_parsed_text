name: flutter_parsed_text_example
description: A example project for flutter_parsed_text.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^5.0.3
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_parsed_text:
    path: ../

flutter:
  uses-material-design: true
